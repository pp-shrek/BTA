const word1 = "Apple";
const word2 = "Mango";

if (word1.length > word2.length) {
    console.log(word1,' is longer word than ', word2);
} else if (word2.length > word1.length) {
    console.log(word2,' is longer word than ', word1);
} else {
    console.log(word1,' length is equal than ', word2);
} // if